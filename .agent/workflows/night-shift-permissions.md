# Night‑Shift Allowed Commands

The following commands must be whitelisted in the agent’s Allow List for the Night‑Shift workflow to run without interruptions:

- git
- npm
- npx
- node
- yarn
- /triage
- /perf
- /evolve
