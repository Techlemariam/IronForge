name: Deploy to Coolify

on:
  release:
    types: [published]
  workflow_dispatch:
  push:
    branches:
      - chore/remove-vercel

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Trigger Coolify Deployment
        run: |
          curl "${{ secrets.COOLIFY_WEBHOOK }}"
